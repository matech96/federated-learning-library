language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install codecov pytest-cov mypy 
# command to run tests
script:
  - pytest -v --cov
  - mypy FLL
  - pylint --fail_under=10 fll
after_success:
  - codecov